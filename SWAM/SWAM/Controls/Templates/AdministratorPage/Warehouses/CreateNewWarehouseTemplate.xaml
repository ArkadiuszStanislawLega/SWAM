<UserControl x:Class="SWAM.Controls.Templates.AdministratorPage.Warehouses.CreateNewWarehouseTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWAM.Controls.Templates.AdministratorPage.Warehouses"
             xmlns:temp="clr-namespace:SWAM.Controls.Templates"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="10"
                    Background="{StaticResource GraphiteBrash}">
            <Label Content="Nazwa magazynu" Foreground="{StaticResource FontsBrash}"/>
            <TextBox Name="Name" Tag="Nazwa magazynu" Style="{StaticResource UserLogin }"/>
        </StackPanel>
        <temp:AddressTemplate x:Name="Address"/>
        
        <Button Name="Confirm" Click="Confirm_Click" 
                Width="200" Content="Dodaj magazyn" 
                Style="{StaticResource NavigationButton}"/>
    </StackPanel>
</UserControl>
