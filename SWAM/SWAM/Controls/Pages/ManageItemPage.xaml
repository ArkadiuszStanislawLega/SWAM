<UserControl x:Class="SWAM.ManageItemPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"       
      mc:Ignorable="d" 
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      Background="{StaticResource BackgroundOfPagesBrash}"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Name="Title" Text="Manager produktów" Grid.Row="0" HorizontalAlignment="Center" FontSize="25" Margin="5"/>

        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>                
                <ColumnDefinition Width="auto"/>                
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" Grid.Column="0"
                      CanUserAddRows="False" SelectionMode="Single" IsReadOnly="True" Style="{StaticResource BasicDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id produktu" Binding="{Binding Id_product}"/>
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Szerokość" Binding="{Binding Width}"/>
                    <DataGridTextColumn Header="Długość" Binding="{Binding Length}"/>
                    <DataGridTextColumn Header="Waga" Binding="{Binding Weight}"/>
                    <DataGridTextColumn Header="Cena sprzedaży" Binding="{Binding Selling_price}"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Vertical" Grid.Column="1" Margin="10 0 0 0">
                <Button Name="AddButton" Margin="10" Content="Dodaj" Width="50" Click="AddButton_Click" Style="{StaticResource NavigationButton}"/>
                <Button Name="EditButton" Margin="10" Content="Edytuj" Width="50" Click="EditButton_Click" Style="{StaticResource NavigationButton}"/>
                <Button Name="DeleteButton" Margin="10" Content="Usuń" Width="50" Click="DeleteButton_Click" Style="{StaticResource NavigationButton}"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
