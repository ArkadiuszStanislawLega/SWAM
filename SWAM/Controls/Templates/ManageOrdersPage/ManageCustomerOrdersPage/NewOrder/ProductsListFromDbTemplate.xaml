<UserControl x:Class="SWAM.Controls.Templates.ManageOrdersPage.NewOrder.ProductsListFromDbTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWAM.Controls.Templates.ManageOrdersPage.NewOrder"
             xmlns:prodLVM="clr-namespace:SWAM.Models.ManageOrdersPage"
             Width="auto"
             MinHeight="300"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d">
    <StackPanel>
        <DataGrid Grid.Column="1" Grid.Row="1"
                  Name="States"
                  LoadingRow="NumberRowIteration"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  DataContext="{x:Static prodLVM:UserDependsAccessProductListViewModel.Instance}"
                  ItemsSource="{Binding States}"
                  Style="{StaticResource BasicDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa" MaxWidth="200" Binding="{Binding Product.Name}"/>
                <DataGridTextColumn Header="Szerokość" Binding="{Binding Product.Width}"/>
                <DataGridTextColumn Header="Długość" Binding="{Binding Product.Length}"/>
                <DataGridTextColumn Header="Waga" Binding="{Binding Product.Weigth}"/>
                <DataGridTextColumn Header="Cena" Binding="{Binding Product.Price}"/>
                <DataGridTextColumn Header="Dostępna ilość" Binding="{Binding Available}"/>
                <DataGridTemplateColumn Header="Dodaj">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Button Width="45" Style="{StaticResource BasicFunctionalButtons}"
                                        Click="AddToShoppingCart_Click">
                                    <TextBlock TextAlignment="Center" Text="+" Margin="15 0 0 0"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
